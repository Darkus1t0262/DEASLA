def send_push(device_id: str, title: str, body: str) -> bool:
    # Simulate sending push notification (use Firebase, etc. in prod)
    print(f"Sending push to {device_id} | {title}: {body}")
    return True
